export default class Fitcurve {
    constructor() {
        this.curve_color = "#00000";

        this.data_set = null;
        this.interval = []; // x min / x max

        this.curve = 0; // pensar na implementação
    }

    //Levenberg–Marquardt
    static regression(data, equation_str, ) {

    }
}